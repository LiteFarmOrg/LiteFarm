extends ../template
block content
    p
        span=`${t('ACCEPT_INVITE_CONFIRMATION.CONGRATS')} `
        strong #{t(`COMMON.${role}`)}
        span=` ${t('ACCEPT_INVITE_CONFIRMATION.AT')} `
        strong #{farm_name}
        | !
    p=`${t('ACCEPT_INVITE_CONFIRMATION.CLICK_BELOW')}:`
    .button-aligner
        a#email-button(href=`${buttonLink}` target='_blank' data-cy='congrats-email-logIn')
            button.button=`${t('COMMON.LOG_IN')}`

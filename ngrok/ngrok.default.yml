# Create a copy of this file called "ngrok.yml" and fill in all ? characters with 
# the appropriate tokens, api keys, etc 
tunnels:
    webapp:
        addr: 3000
        proto: http
    api:
        addr: 5001
        proto: http
version: "2"
region: us
authtoken: ?
